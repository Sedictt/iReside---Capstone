# React Doctor False Positives

## server-auth-actions

These are intentionally public endpoints or ARE the auth utilities themselves:

| File | Function | Reason |
|------|----------|--------|
| `src/lib/supabase/auth.ts:12` | `auth()` | This IS the auth check utility - it validates session internally |
| `src/lib/supabase/auth.ts:25` | `requireUser()` | This IS a higher-security auth check utility |
| `src/lib/supabase/auth.ts:39` | `signUp()` | Public endpoint for new user registration - must NOT require auth |
| `src/lib/supabase/auth.ts:65` | `signIn()` | Public endpoint for user login - must NOT require auth |

## effect-needs-cleanup

| File | Line | Status |
|------|------|--------|
| `src/context/NotificationContext.tsx:270` | 270 | ✅ Fixed - Added `return () => {}` for early return path |

